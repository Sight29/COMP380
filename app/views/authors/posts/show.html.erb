<%  provide(:page_title, @post.title) %>
  <% provide(:author, 'active') %>

  <div style='margin-top: 10px;'>
   <%= link_to 'Edit', edit_authors_post_path(@post), class: 'btn  btn-warning' %>
   <%= link_to 'Delete', authors_post_path(@post), method: :delete, data: {confirm: 'Are you sure u want to remove?'},class: 'btn  btn-danger'  %>   
  </div>
    <div class="row">
      <div class="col-md-1 col-lg-2"></div>
      <div class="col-md-12 col-lg-10 text-xs-center">
        <h1>
          <%= @post.title %>
        </h1>
        <p> <%= @post.display_published_day %></p>
        <%= @post.body %>
      </div>
    </div>